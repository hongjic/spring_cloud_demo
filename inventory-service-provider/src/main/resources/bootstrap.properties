### This information needs to happen before the rest of the applications
### information is evaluated, it needs to happen in the bootstrap.
spring.application.name=inventory-service
spring.cloud.config.uri=http://localhost:8888
management.security.enabled=false